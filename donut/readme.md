интерактивную визуализацию процесса решения задачи выполнимости булевых формул (SAT) с использованием метафоры "вращающихся бубликов". Это оригинальный подход, который представляет логическую структуру формулы как динамическую физическую систему, где каждая переменная — это вращающееся кольцо с цветными узлами, а процесс поиска решения визуализируется как синхронизация осцилляций.

🧩 Архитектура программы
1. Двухкомпонентная структура
Программа состоит из двух основных частей:

A. DIMACS загрузчик (parse_dimacs_cnf, load_benchmark_folder)
Парсит файлы в стандартном формате DIMACS CNF

Извлекает количество переменных и список дизъюнктов

Поддерживает пакетную загрузку из папки с бенчмарками

Предоставляет статистику по загруженным формулам

B. Визуализатор "Вращающиеся Бублики" (DonutSATVisualizer)
Реализует метафору вращающихся колец

Создает анимацию динамики SAT-формулы

Отображает процесс синхронизации в реальном времени

2. Классы и их ответственность
Компонент	Назначение
parse_dimacs_cnf	Парсинг одного CNF файла
load_benchmark_folder	Загрузка всех CNF из папки
print_benchmark_info	Вывод статистики формулы
DonutSATVisualizer	Основной класс визуализации
main	Точка входа, orchestration
🔬 Детальная реализация визуализатора
2.1 Инициализация и подготовка данных
python
def __init__(self, n_vars, clauses, benchmark_name):
При создании визуализатор:

Сохраняет количество переменных и список дизъюнктов

Строит индекс var_to_clauses для быстрого доступа к клаузам каждой переменной

Классифицирует клаузы на Horn, Dual-Horn и XOR

Инициализирует значения переменных случайными числами в диапазоне [-1, 1]

Создает фазы вращения для каждой переменной

Настраивает позиции бубликов на сетке

2.2 Классификация клауз
python
def classify_clauses(self):
Алгоритм классификации:

Horn-клаузы: максимум одна положительная литера (красный цвет)

Dual-Horn: максимум одна отрицательная литера (синий цвет)

XOR-структуры: дизъюнкты длины 3 с двумя отрицаниями (зеленый цвет)

Остальные: серый цвет

Эта классификация важна для визуального анализа структуры формулы.

2.3 Построение узлов переменных
python
def build_variable_nodes(self):
Для каждой переменной:

Находим все клаузы, в которые она входит

Равномерно распределяем их по окружности бублика

Сохраняем пары (индекс_клаузы, угол) для последующего вращения

2.4 Динамическая модель
Сердце симуляции — функция обновления динамики:

python
def update_dynamics(self):
Физическая интерпретация:
Каждая переменная имеет:

Текущее значение в диапазоне [-1, 1] (где >0 соответствует True, <0 соответствует False)

Фазу вращения, определяющую, какой узел находится в верхней позиции

Силы, действующие на переменные:
Давление от неудовлетворенных клауз:

python
if sat_degree < 0.8:  # Клауза плохо удовлетворена
    target = 1 if lit > 0 else -1
    pressures[var] += (1 - sat_degree) * 0.3 * (target - np.tanh(self.var_values[var]))
Клауза "давит" на свои переменные, пытаясь изменить их знак

Сила давления пропорциональна степени неудовлетворенности

Влияние активного узла:

python
active_idx = min(range(len(rotated_angles)), 
                key=lambda i: abs(rotated_angles[i] - np.pi/2))
Узел, оказавшийся в верхней точке, оказывает дополнительное влияние

Создает эффект "вращающегося прожектора"

Уравнение движения:

python
self.var_values[var] = damping * self.var_values[var] + inertia * np.tanh(pressures[var])
damping = 0.97 — затухание (инерционность системы)

inertia = 0.1 — чувствительность к давлению

np.tanh — нелинейное ограничение давления

Мера синхронизации:
RMS (Root Mean Square) отклонение вычисляется как:

python
rms = np.sqrt(np.mean((self.var_values - mean_values)**2))
Показывает, насколько сильно колеблются значения переменных

Приближение к решению → уменьшение осцилляций → RMS → 0

2.5 Визуализация бублика
python
def draw_donut(self, ax, center_x, center_y, var_idx):
Каждый бублик содержит:

Внешний круг — граница переменной

Внутренний круг (дырка) — стилизованный элемент

Заливка центра — текущее значение переменной:

Красный → положительное значение (ближе к True)

Зеленый → отрицательное значение (ближе к False)

Интенсивность цвета отражает величину

Узлы на окружности — клаузы, в которые входит переменная:

Цвет узла = тип клаузы (красный/синий/зеленый/серый)

Размер узла = 1 - степень удовлетворенности (больше = активнее)

Позиция = базовая позиция + фаза вращения

Подсветка активного узла — желтым ореолом

2.6 Анимация
python
def animate(self, frame):
Функция анимации:

Очищает оба графика

Обновляет динамику системы

Отрисовывает все бублики на левой панели

Добавляет легенду и заголовок

Строит график RMS на правой панели:

Синяя линия — сырые данные

Красная пунктирная — сглаженный тренд

Зеленая отметка при обнаружении решения

🎨 Визуальные элементы и их значение
Элемент	Что обозначает
🍩 Бублик	Переменная
🔴 Красный узел	Horn-клауза
🔵 Синий узел	Dual-Horn клауза
🟢 Зеленый узел	XOR-структура
⚪ Серый узел	Другой тип клаузы
🟡 Желтая подсветка	Активный узел (в верхней точке)
🌈 Заливка центра	Текущее значение переменной
📈 График справа	Процесс синхронизации
🔄 Процесс поиска решения
Начальное состояние: случайные значения, хаотичные осцилляции

Вращение: узлы перемещаются по окружности, меняются активные влияния

Давление: неудовлетворенные клаузы "подталкивают" переменные

Синхронизация: по мере приближения к решению осцилляции затухают

Решение: когда RMS < 0.05 и все клаузы удовлетворены — 🎉 ПОБЕДА!

📊 Математическая модель
Дискретно-непрерывная гибридная система
Дискретная часть: структура клауз и их типы

Непрерывная часть: значения переменных в [-1, 1]

Уравнения динамики
Для переменной 
x
i
x 
i
​
 :

d
x
i
d
t
=
−
λ
x
i
+
η
tanh
⁡
(
∑
c
∈
C
i
w
c
⋅
sign
(
l
i
,
c
)
⋅
(
1
−
sat
c
)
)
dt
dx 
i
​
 
​
 =−λx 
i
​
 +ηtanh( 
c∈C 
i
​
 
∑
​
 w 
c
​
 ⋅sign(l 
i,c
​
 )⋅(1−sat 
c
​
 ))
где:

λ
λ — коэффициент затухания (damping)

η
η — инерция (inertia)

C
i
C 
i
​
  — множество клауз с переменной 
i
i

l
i
,
c
l 
i,c
​
  — знак литеры в клаузе

sat
c
sat 
c
​
  — степень удовлетворения клаузы

Мера сходимости
RMS
(
t
)
=
1
N
∑
i
=
1
N
(
x
i
(
t
)
−
x
ˉ
i
(
t
)
)
2
RMS(t)= 
N
1
​
  
i=1
∑
N
​
 (x 
i
​
 (t)− 
x
ˉ
  
i
​
 (t)) 
2
 
​
 
где 
x
ˉ
i
(
t
)
x
ˉ
  
i
​
 (t) — скользящее среднее за последние 20 шагов.

💡 Инновационные аспекты
Метафорическая визуализация — сложная логическая задача представлена как интуитивно понятная физическая система

Цветовая классификация — позволяет сразу видеть структуру формулы

Динамика вращения — создает эффект "сканирующего влияния"

Непрерывная логика — значения переменных не бинарны, а плавно меняются

Визуальная обратная связь — процесс синхронизации наглядно показывает приближение к решению

🚀 Практическое применение
Образовательное: демонстрация принципов работы SAT-солверов

Исследовательское: визуальный анализ структуры сложных формул

Демонстрационное: красивая визуализация для лекций и презентаций

📈 Возможные улучшения
Интерактивность: возможность влиять на параметры в реальном времени

Сохранение анимации: экспорт в видеофайл

Дополнительные метрики: энергия системы, энтропия

Масштабирование: оптимизация для формул с тысячами переменных

Звуковая обратная связь: озвучивание процесса синхронизации

🎯 Заключение
Созданный визуализатор превращает абстрактную логическую задачу в захватывающее динамическое представление. "Вращающиеся бублики" не только красивы, но и информативны — они позволяют наблюдать за процессом самоорганизации системы, стремящейся к выполнению всех логических условий. Это уникальный инструмент, сочетающий эстетику, математику и информатику.
