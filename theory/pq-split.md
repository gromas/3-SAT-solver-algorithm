# 🔬 Теоретическая основа PQ-алгоритма

**Автор:** [Golubin Roman]  
**Год:** 2026  

---

## 🎯 Ключевая идея: вершинное покрытие и структурное разложение

Основная проблема SAT-решателей — экспоненциальный взрыв при переборе.  
PQ-алгоритм обходит эту проблему, используя **структурные свойства** исходной 3-КНФ.

### Определение 1: Граф взаимодействия переменных

Для формулы F в 3-КНФ построим граф G = (V, E):
- Вершины V — переменные (x_1, ..., x_n)
- Ребро (x_i, x_j) ∈ E, если существует клоз, содержащий одновременно x_i и x_j

### Определение 2: Вершинное покрытие

Множество P ⊂ V называется **вершинным покрытием**, если каждое ребро графа G инцидентно хотя бы одной вершине из P.

---

## 💡 Ключевое наблюдение

Если зафиксировать значения всех переменных из вершинного покрытия P, то оставшаяся формула становится **2-КНФ**!

### Почему это работает:

- В 3-КНФ каждый клоз содержит три литерала.
- Если переменная из P есть в клозе, то после её фиксации клоз либо исчезает (становится истинным), либо упрощается до 2-КНФ.
- Так как P покрывает все рёбра, **каждый клоз содержит хотя бы одну переменную из P**.
- Следовательно, после фиксации P **все клозы становятся не длиннее двух литералов**.

---

## 📦 Разложение P и Q

Пусть Q = V ∖ P — оставшиеся переменные (не вошедшие в покрытие).

### Свойства:

- |P| + |Q| = n
- Q — независимое множество в графе G (нет рёбер между переменными из Q)
- Любой клоз содержит **хотя бы одну переменную из P**
- При фиксации P получается 2-КНФ от переменных Q

---

## ⚖️ Две стратегии в зависимости от размеров P и Q

### Случай 1: |P| ≤ |Q|

**Явный перебор по P**:
- Перебираем все 2^|P| назначений переменных из P
- Для каждого назначения решаем 2-КНФ за O(m), где m — число клозов
- Если хоть одно назначение дало выполнимую 2-КНФ — F выполнима

**Сложность:** O(m * 2^|P|)

### Случай 2: |P| > |Q|

**Символьная элиминация P через BDD**:
- Строим BDD для каждой переменной из P, используя клозы, где она минимальна
- Последовательно элиминируем переменные P от больших индексов к малым через композицию и ∃-квантификацию
- В результате получаем BDD только от переменных Q, представляющий **проекцию** исходной формулы на Q
- Проверяем BDD на выполнимость

**Сложность:** O(|P|^2 * 2^|Q|) с учётом кеширования BDD-операций

---

## 📐 Теорема о сложности

**Теорема 1.** Для любой 3-КНФ от (n) переменных существует разложение {P, Q} на вершинное покрытие и независимое множество, такое что:

Время работы PQ-алгоритма = O(min(2^|P|, 2^|Q|) * poly(n), где (min(|P|, |Q|) ≤ n/2).

**Следствие 1.** PQ-алгоритм гарантированно работает за время O(2^(n/2) * poly(n)) на любой 3-КНФ.

**Следствие 2.** Если граф взаимодействия переменных имеет вершинное покрытие размера k ≪ n/2, алгоритм работает за O(2^k * poly(n)).

---

## 🔬 Почему это работает на практике

Реальные задачи (особенно из области верификации, планирования, комбинаторики) часто имеют **структуру**, допускающую **малое вершинное покрытие**.

### Примеры:
- Графы ограниченной древовидной ширины (treewidth)
- Задачи с разреженными графами взаимодействия
- Формулы, моделирующие цепи (circuits)
- Проблема "pigeonhole" и другие комбинаторные задачи

В таких случаях |P| оказывается **значительно меньше** n/2, и алгоритм работает значительно быстрее полного перебора.

---

## 📊 Сравнение с классическими подходами

| Метод | Сложность в худшем случае | Сложность при малом P |
|-------|---------------------------|------------------------|
| Полный перебор | O(2^n) | O(2^n) |
| DPLL / CDCL | O(2^n) в худшем | Экспонента, но с эвристиками |
| **PQ-алгоритм** | O(2^(n/2)) | (O(2^|P|) |

---

## 🧠 Историческая справка

Идея использовать вершинное покрытие для сведения 3-SAT к 2-SAT была открыта автором в 2020 году и вынашивалась пять лет до первой работающей реализации.  

Ключевой инсайт пришёл из наблюдения: **если переменные в клозе соединены рёбрами, то фиксация вершинного покрытия "разрывает" все клозы до длины 2**. Это позволяет либо перебирать покрытие явно, либо элиминировать его символически.

Это пример того, как **структурный анализ** может радикально улучшить производительность алгоритмов для NP-полных задач.

---

**Лицензия:** MIT
