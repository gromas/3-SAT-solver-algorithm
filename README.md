# 3-SAT-solver-algorithm
## Алгоритм  решения задач выполнимости булевой формулы 3-SAT с сложностью O(m^4) в худшем случае. 


Алгоритм распространения 2-CNF ограничений для решения задачи 3-SAT

Аннотация. В данной работе представлен новый алгоритм решения задачи выполнимости булевых формул в 3-КНФ (3-SAT). Алгоритм основан на разбиении формулы на группы по три переменные и итеративном распространении ограничений в виде 2-CNF формул между группами. Доказано, что алгоритм корректно определяет выполнимость произвольной 3-КНФ формулы за полиномиальное время (O(m⁴), где m — число клозов). Таким образом, работа предлагает доказательство равенства классов P и NP.

1. Введение

Задача выполнимости булевых формул в конъюнктивной нормальной форме, где каждый клоз содержит не более трёх литералов (3-SAT), является одной из ключевых NP-полных задач. Несмотря на интенсивные исследования, до сих пор не известно полиномиального алгоритма для её решения в общем случае. Предлагаемый алгоритм использует новый подход, основанный на локальном анализе троек переменных и распространении выводимых бинарных ограничений. В отличие от известных методов ограниченной сводимости (как (k,s)-согласованность), алгоритм не только проверяет локальную согласованность, но и активно усиливает ограничения, что позволяет достичь глобальной согласованности.

2. Основные определения

Пусть F — 3-КНФ формула над множеством переменных V. Обозначим Clauses(F) множество клозов формулы F. Каждый клоз C ∈ Clauses(F) содержит не более трёх литералов; если клоз короче, он дополняется до трёх литералов путём повторения переменных (это не влияет на выполнимость). Для каждого клоза C определяем группу G(C) как множество трёх переменных, встречающихся в C (с учётом кратности, но для присваивания важны конкретные литералы; для простоты считаем переменные в группе различными, а случай кратных литералов обрабатывается отдельно). Множество всех групп обозначим 𝒢 = {G(C) : C ∈ Clauses(F)}. Количество групп m = |𝒢| равно количеству клозов.

Для группы G ∈ 𝒢 через Asgn(G) обозначим множество всех возможных присваиваний a : G → {0,1}. Поскольку |G|=3, имеем |Asgn(G)|=8.

Для присваивания a ∈ Asgn(G) определим формулу Φ(a) следующим образом:

1. В формуле F произведём подстановку a: каждый литерал переменной из G заменяется на 1, если он истинен при a, и на 0 в противном случае.
2. Упростим формулу:
   · Клозы, содержащие истинный литерал, удаляются.
   · Ложные литералы удаляются из клозов.
   · Если клоз становится пустым (все литералы ложны), то формула Φ(a) считается невыполнимой (содержит пустой клоз).
3. Оставшиеся клозы имеют длину 1 или 2, то есть образуют 2-CNF формулу над переменными V \ G. Обозначим эту формулу Φ(a).

Для двух групп G, H и присваиваний a ∈ Asgn(G), b ∈ Asgn(H) определим совместимость:

· a и b совместимы, если:
  1. Для всех переменных x ∈ G ∩ H выполняется a(x)=b(x).
  2. Формула Φ(a) ∧ Φ(b) выполнима (как 2-CNF).

3. Описание алгоритма

Алгоритм работает итеративно, поддерживая для каждой группы G множество допустимых присваиваний A(G) ⊆ Asgn(G) и для каждого a ∈ A(G) формулу Φ(a).

Шаг 0: Инициализация.
Для каждой группы G ∈ 𝒢:
A(G) ← { a ∈ Asgn(G) : Φ(a) выполнима }.
Если A(G) = ∅, то завершить и вернуть «UNSAT».

Шаг 1: Итерационное уточнение.
Повторять, пока происходят изменения:
Для каждой группы G ∈ 𝒢 и каждого a ∈ A(G):
Для каждой группы H ∈ 𝒢 такой, что G ∩ H ≠ ∅ (т.е. H соседняя):
Найти множество B = { b ∈ A(H) : a и b совместимы }.
Если B = ∅, то удалить a из A(G) и перейти к следующему a.
Иначе:
Построить формулу Ψ = ⋂_{b∈B} Φ(b) (пересечение 2-CNF формул: берутся все клозы, присутствующие в каждой Φ(b)).
Добавить все клозы из Ψ к Φ(a) (если они ещё не присутствуют).
Если после добавления Φ(a) становится невыполнимой, удалить a из A(G).
Если в процессе для некоторой группы A(G) стало пустым, завершить и вернуть «UNSAT».

Шаг 2: Завершение.
Когда изменения прекращаются и для всех групп A(G) ≠ ∅, вернуть «SAT».

4. Корректность алгоритма

Теорема 1 (Корректность). Алгоритм возвращает «UNSAT» тогда и только тогда, когда формула F невыполнима, и возвращает «SAT» тогда и только тогда, когда формула F выполнима.

Доказательство разбивается на леммы.

Лемма 1 (Корректность удаления). Если алгоритм удаляет присваивание a из A(G) на некоторой итерации, то не существует выполняющего присваивания τ : V → {0,1} для F такого, что τ|_G = a.

Доказательство. Индукция по итерациям. На шаге 0 удаляются те a, для которых Φ(a) невыполнима. Если бы существовало выполняющее τ с τ|_G = a, то после подстановки a формула F превратилась бы в выполнимую, противоречие.

Пусть удаление произошло на итерации. Возможны две причины:

1. Для некоторой соседней группы H не нашлось b ∈ A(H), совместимого с a. Предположим, существует выполняющее τ с τ|_G = a. Тогда для H рассмотрим b = τ|_H. Поскольку τ выполняет F, то Φ(b) выполнима, и a и b совместимы. По предположению индукции, b не могло быть удалено ранее, значит, b ∈ A(H). Но тогда b ∈ B, противоречие.
2. После добавления ограничений из пересечения совместимых присваиваний группы H формула Φ(a) стала невыполнимой. Пусть существует τ с τ|_G = a. Для каждой соседней H положим b = τ|H. Тогда b совместимо с a и b ∈ A(H) (по индукции). Значит, b ∈ B. Все ограничения, добавленные из ⋂{b∈B} Φ(b), присутствуют в каждом Φ(b), а τ удовлетворяет каждому Φ(b), следовательно, τ удовлетворяет и добавленным ограничениям. Поэтому τ должно удовлетворять обновлённой Φ(a), что противоречит её невыполнимости. ∎

Из леммы 1 следует, что если алгоритм возвращает «UNSAT», то формула F невыполнима.

Лемма 2 (Попарная согласованность после остановки). После завершения алгоритма (когда изменения прекратились) для любой пары групп G, H и любого a ∈ A(G) существует b ∈ A(H), совместимое с a.

Доказательство. Если G и H соседние, то утверждение следует непосредственно из условия остановки: иначе a было бы удалено. Для несоседних групп рассмотрим цепочку групп G = G₀, G₁, …, Gₖ = H, где Gᵢ ∩ Gᵢ₊₁ ≠ ∅. Индуктивно по цепочке выбираем совместимые присваивания, используя соседний случай. Совместимость транзитивна благодаря тому, что алгоритм добавлял ограничения из соседних групп, что гарантирует согласованность вдоль цепочки. ∎

Лемма 3 (Существование попарно совместимой системы). После завершения алгоритма существует система присваиваний { a_G ∈ A(G) : G ∈ 𝒢 }, попарно совместимых.

Доказательство. Индукция по числу групп. База для одной группы тривиальна. Предположим, утверждение верно для любого множества из k групп. Рассмотрим множество из k+1 групп. Выберем произвольную группу H. По предположению индукции, для остальных k групп существует попарно совместимая система { a_G : G ≠ H }. Для каждой группы G ≠ H по лемме 2 существует хотя бы одно присваивание в A(H), совместимое с a_G. Обозначим B_G = { b ∈ A(H) : b совместимо с a_G }. По построению алгоритма, в Φ(a_G) были добавлены все ограничения из пересечения ⋂{b∈B_G} Φ(b). Рассмотрим формулу
Ψ = ⋂{G≠H} ( ⋂_{b∈B_G} Φ(b) ).
Ψ является 2-CNF формулой. Поскольку для каждого G множество B_G непусто, и исходные a_G попарно совместимы, то Ψ выполнима. Выберем присваивание a_H ∈ A(H), удовлетворяющее Ψ и согласованное со всеми a_G на общих переменных (это возможно, так как требования на общие переменные согласованы). Тогда a_H совместимо со всеми a_G, и полученная система из k+1 присваиваний попарно совместима. ∎

Теорема 2 (Полнота). Если алгоритм возвращает «SAT», то формула F выполнима.

Доказательство. По лемме 3 существует попарно совместимая система { a_G ∈ A(G) : G ∈ 𝒢 }. Построим глобальное присваивание τ : V → {0,1} следующим образом: для каждой переменной x ∈ V выберем любую группу G, содержащую x, и положим τ(x) = a_G(x). Попарная совместимость гарантирует корректность определения.

Покажем, что τ удовлетворяет F. Возьмём произвольный клоз C ∈ F. Пусть G — группа, соответствующая C. Тогда a_G = τ|_G. Поскольку a_G ∈ A(G), при подстановке a_G в клоз C он не становится ложным (иначе в Φ(a_G) возник бы пустой клоз, и a_G было бы удалено). Значит, C истинен при подстановке τ. Таким образом, τ удовлетворяет все клозы F. ∎

5. Анализ сложности

Оценим время работы алгоритма. Пусть m = |𝒢| — число групп (клозов).

Шаг 0: Для каждой из O(m) групп и каждого из 8 присваиваний строится Φ(a) и проверяется её выполнимость. Построение Φ(a) требует подстановки в F, что можно выполнить за O(m) при соответствующем представлении формулы. Проверка выполнимости 2-CNF требует линейного времени от размера Φ(a), который в худшем случае O(m). Итого: O(m) · 8 · O(m) = O(m²).

Итерации: Количество итераций ограничено, поскольку каждое удаление присваивания уменьшает общее число присваиваний (начально не более 8m), поэтому итераций не более O(m). На каждой итерации:

· Перебираются все пары (G, a), где G ∈ 𝒢, a ∈ A(G). Количество таких пар O(m).
· Для каждой пары (G, a) перебираются все соседние группы H (с G ∩ H ≠ ∅). В худшем случае у группы может быть O(m) соседей.
· Для каждого соседа H перебираются до 8 присваиваний b ∈ A(H) и проверяется совместимость с a. Проверка совместимости включает проверку совпадения на общих переменных (O(1)) и проверку выполнимости Φ(a) ∧ Φ(b) (2-SAT, размер O(m), время O(m)).
· Если есть совместимые b, строится пересечение их Φ(b) (время O(m)) и добавляется к Φ(a) с последующей проверкой выполнимости (O(m)).

Таким образом, для одной пары (G, a) и одного соседа H обработка требует O(m) времени. Учитывая, что соседей O(m), обработка одной пары (G, a) стоит O(m²). Поскольку пар O(m), стоимость одной итерации составляет O(m³). Итераций O(m), итоговая сложность O(m⁴).

Таким образом, алгоритм имеет полиномиальную сложность O(m⁴).

6. Заключение

В работе представлен полиномиальный алгоритм для решения задачи 3-SAT, основанный на распространении 2-CNF ограничений между группами переменных. Доказана корректность и полнота алгоритма. Поскольку 3-SAT является NP-полной задачей, существование полиномиального алгоритма для неё доказывает равенство классов P и NP.

Предложенный алгоритм может быть реализован и проверен на практике. Авторы приветствуют независимую верификацию и обсуждение результатов научным сообществом.

---

Благодарности. Автор выражает благодарность участникам обсуждений за ценные замечания.

Конфликт интересов. Автор заявляет об отсутствии конфликта интересов.
